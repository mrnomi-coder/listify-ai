import Link from "next/link";
import { ArrowRight, CheckCircle2, CloudUpload, FileText, ShoppingBag, Sparkles } from "lucide-react";
import { ThemeToggle } from "@/components/theme-toggle";
import { Badge, buttonVariants, Card, CardContent } from "@/components/ui";
import { cn } from "@/lib/utils";

const workflow = [{ label: "Upload", icon: CloudUpload }, { label: "Analyze", icon: Sparkles }, { label: "Rewrite", icon: FileText }, { label: "Publish", icon: ShoppingBag }];

export default function LandingPage() {
  return <div className="min-h-screen bg-background"><header className="fixed inset-x-0 top-0 z-40 border-b border-white/15 bg-background/45 backdrop-blur-xl"><div className="container flex h-16 items-center justify-between"><Link href="/" className="flex items-center gap-2 font-semibold text-white md:text-foreground"><span className="flex h-9 w-9 items-center justify-center rounded-md bg-primary text-primary-foreground"><Sparkles className="h-4 w-4" /></span>Listify AI</Link><nav className="hidden items-center gap-6 text-sm text-white/82 md:flex"><Link href="#workflow">Workflow</Link><Link href="#platform">Platform</Link><Link href="/billing">Pricing</Link></nav><div className="flex items-center gap-2"><ThemeToggle /><Link className={cn(buttonVariants({ size: "sm" }), "hidden sm:inline-flex")} href="/dashboard">Open app</Link></div></div></header><section className="relative flex min-h-[92svh] items-end overflow-hidden"><img src="https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?auto=format&fit=crop&w=2200&q=85" alt="" className="absolute inset-0 h-full w-full object-cover" /><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(8,13,18,0.88),rgba(8,13,18,0.62),rgba(8,13,18,0.18))]" /><div className="container relative z-10 pb-16 pt-28 text-white"><Badge className="mb-5 border-white/20 bg-white/12 text-white">AI listing automation for Shopify</Badge><h1 className="max-w-3xl text-5xl font-semibold sm:text-6xl lg:text-7xl">Listify AI</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-white/82">Turn product photos, supplier URLs, and rough notes into polished Shopify listings with market research, SEO fields, brand voice rewriting, and one-click publishing.</p><div className="mt-8 flex flex-wrap gap-3"><Link className={cn(buttonVariants({ size: "lg" }), "bg-white text-slate-950 hover:bg-white/90")} href="/generator">Generate a listing<ArrowRight className="h-4 w-4" /></Link><Link className={cn(buttonVariants({ size: "lg", variant: "outline" }), "border-white/30 bg-white/10 text-white hover:bg-white/16")} href="/bulk-upload">Process bulk images</Link></div><div className="mt-10 grid max-w-3xl gap-3 sm:grid-cols-3">{["Vision analysis", "SEO research", "Shopify CSV/API"].map((item) => <div key={item} className="flex items-center gap-2 text-sm text-white/86"><CheckCircle2 className="h-4 w-4 text-primary" />{item}</div>)}</div></div></section><section id="workflow" className="border-b py-14"><div className="container grid gap-4 md:grid-cols-4">{workflow.map((item, index) => { const Icon = item.icon; return <Card key={item.label} className="animate-fade-up" style={{ animationDelay: `${index * 90}ms` }}><CardContent className="p-5"><span className="flex h-11 w-11 items-center justify-center rounded-md bg-primary/10 text-primary"><Icon className="h-5 w-5" /></span><p className="mt-5 text-lg font-semibold">{item.label}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Upload images, analyze with vision AI, rewrite with GPT, then export CSV or publish to Shopify.</p></CardContent></Card>; })}</div></section><section id="platform" className="subtle-grid py-16"><div className="container grid gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:items-center"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Production stack</p><h2 className="mt-3 text-3xl font-semibold sm:text-4xl">Built for sellers who ship catalogs, not one-off prompts.</h2><p className="mt-4 leading-7 text-muted-foreground">A modular Next.js application with Prisma, Redis jobs, Cloudinary uploads, OpenAI generation, and Shopify publishing APIs.</p></div><div className="grid gap-4 sm:grid-cols-2">{["Background AI jobs", "Draft and published states", "Confidence scoring", "Reusable brand voices", "Secure auth routes", "Rate-limited APIs"].map((item) => <div key={item} className="rounded-lg border bg-card p-4 text-sm font-medium shadow-sm">{item}</div>)}</div></div></section></div>;
}
