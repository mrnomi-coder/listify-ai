import { CheckCircle2, KeyRound, PlugZap, Send, ShieldCheck } from "lucide-react";
import { AppShell, PageHeading } from "@/components/app-shell";
import { Badge, Button, Card, CardContent, CardHeader, CardTitle, Input } from "@/components/ui";

export default function ShopifyPage() {
  const scopes = ["read_products", "write_products", "read_inventory", "write_inventory"];
  return <AppShell><PageHeading eyebrow="Commerce" title="Shopify integration" description="Connect a Shopify store, validate Admin API scopes, and publish ready listings directly from Listify AI." actions={<Button><PlugZap className="h-4 w-4" />Connect store</Button>} /><div className="grid gap-5 xl:grid-cols-[0.9fr_1.1fr]"><Card><CardHeader><CardTitle>Store connection</CardTitle></CardHeader><CardContent className="space-y-4"><label className="space-y-2 text-sm font-medium">Shopify shop domain<Input defaultValue="northline-supply.myshopify.com" /></label><label className="space-y-2 text-sm font-medium">Admin access token<Input type="password" defaultValue="shpat_************************" /></label><Button className="w-full"><KeyRound className="h-4 w-4" />Save encrypted token</Button></CardContent></Card><Card><CardHeader className="flex flex-row items-center justify-between"><CardTitle>Publishing readiness</CardTitle><Badge variant="success">Connected</Badge></CardHeader><CardContent className="space-y-5"><div className="grid gap-3 sm:grid-cols-2">{scopes.map((scope) => <div key={scope} className="flex items-center gap-2 rounded-lg border p-3 text-sm"><CheckCircle2 className="h-4 w-4 text-emerald-500" />{scope}</div>)}</div><div className="rounded-lg border bg-muted/35 p-4"><div className="flex items-center gap-2 text-sm font-medium"><ShieldCheck className="h-4 w-4 text-primary" />Publish flow</div><p className="mt-2 text-sm leading-6 text-muted-foreground">Ready listings are mapped to Shopify product fields, SEO metadata, tags, images, and active or draft status before publishing.</p></div><Button variant="outline"><Send className="h-4 w-4" />Test publish draft</Button></CardContent></Card></div></AppShell>;
}
